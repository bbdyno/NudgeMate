# CloudKit 범위

현재 릴리스는 로컬 SwiftData 저장만 지원하며 iCloud/CloudKit 동기화 UI를 노출하지 않는다. 외부 컨테이너가 준비되지 않은 상태에서 동기화를 제공하는 것처럼 표시하지 않기 위한 결정이다.

향후 도입 시 별도 이슈에서 컨테이너 식별자, 충돌 정책, 삭제 전파, 구독 만료 시 데이터 보존, 오프라인 재시도, 마이그레이션 및 다중 기기 테스트를 먼저 정의해야 한다.
