# NudgeMate

iOS 17 이상을 지원하는 SwiftUI 일정 넛지 앱입니다. 프로젝트 파일은 Tuist로 생성합니다.

```sh
tuist generate
```

문자열 리소스는 한국어를 개발 언어로 하며 영어, 중국어 간체, 중국어 번체, 일본어를 함께 지원합니다. `Project.swift`의 `.strings()` 리소스 신시사이저가 `NudgeMateStrings` 접근자를 생성합니다.
